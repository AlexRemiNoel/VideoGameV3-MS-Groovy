server.port=8084 # Or another unique port

# MongoDB Configuration (AI4)
spring.data.mongodb.uri=mongodb://localhost:27017/user_dashboard_db # Adjust as needed

# Downstream Service URLs (for Domain Client Layer AI3)
services.user.url=http://localhost:8081/api/v1/user # Adjust port for UserService
services.game.url=http://localhost:8082/api/v1/game # Adjust port for GameService
services.download.url=http://localhost:8083/api/v1/downloads # Adjust port for DownloadService

# For consistent Jackson date handling
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss.SSSXXX
spring.jackson.time-zone=UTC